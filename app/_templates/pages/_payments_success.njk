{% from "../structure/_header.njk" import header %}
{% from "../structure/_success.njk" import success %}

<section class="page-pay pay-success">
	{{ header({
		link: {
            text: 'volver a Inicio',
            href: '/index.html'
        }
	}) }}

    {{ success({
        title: '¡El registro del pago se generó con éxito!',
        subtitle: 'Ingresando a la opción <strong>Tu actividad</strong> podés consultar los <strong>comprobantes</strong>.'
    }) }}
</section>