{% macro header(options)%}
	<div class="flex-column page-header">
		<div class="page-section">
			{% if options.section %}
				<p class="top-decoration">{{ options.section | safe }}</p>
			{% elseif options.link %}
				<a class="top-decoration" href="{{ options.link.href }}">{{ options.link.text }}</a>
			{% endif %}
		</div>
	</div>
{% endmacro%}